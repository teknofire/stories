%table.table.table-striped
  %thead
    %tr
      %th Name
      %th Size
      %th Status
      %th.text-right Remove
  %tbody
    - uploads.each do |upload|
      - next if upload.new_record?
      %tr
        %td= link_to upload.file_filename, upload
        %td= number_to_human_size upload.file_size
        %td= upload.status
        %td.text-right
          = link_to upload, method: :delete, data: { confirm: 'Are you sure?' }, class: 'btn btn-danger btn-sm' do
            &times;
